version: 2
updates:
  # Keep the GitHub Actions used by the WASM build/lint workflows current.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    labels:
      - dependencies
      - ci
